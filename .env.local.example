# ============================================================================
# Supabase Database Configuration
# ============================================================================
#
# Get these values from: https://app.supabase.com/project/[project-id]/settings/database
#
# To find your credentials:
# 1. Go to Supabase Dashboard
# 2. Select your project
# 3. Go to Settings > Database
# 4. Look for "Connection string" section
# 5. Or scroll down to see individual connection parameters
#

# Database host (either direct or pooler)
# Direct: qcwnlbpultscerwdnzbm.supabase.co
# Pooler: aws-0-xxx.pooler.supabase.com (recommended for migrations)
DB_HOST=qcwnlbpultscerwdnzbm.pooler.supabase.com

# Database port
# Direct: 5432
# Pooler: 6543
DB_PORT=6543

# Database name (usually 'postgres')
DB_NAME=postgres

# Database user (usually 'postgres')
DB_USER=postgres

# Database password (from Supabase > Settings > Database > Connection parameters)
DB_PASSWORD=your_database_password_here

# ============================================================================
# Supabase API Configuration (for other operations)
# ============================================================================

SUPABASE_PROJECT_ID=qcwnlbpultscerwdnzbm
SUPABASE_ACCESS_TOKEN=sbp_660a64450ab81afe4c6e80fa4b2c8ff5cfd7ee9a

# ============================================================================
# Application Configuration
# ============================================================================

# Frontend URL
VITE_SUPABASE_URL=https://qcwnlbpultscerwdnzbm.supabase.co
VITE_SUPABASE_ANON_KEY=your_anon_key_here
# Optional: set to true to skip Supabase official currency rate lookups and rely on local fallbacks
VITE_DISABLE_OFFICIAL_RATES=false

# ============================================================================
# IMPORTANT SECURITY NOTES
# ============================================================================
#
# 1. NEVER commit .env.local to version control
# 2. Keep DB_PASSWORD secure - it's a super user password
# 3. Use pooler host for migrations (port 6543) for better stability
# 4. Access tokens are sensitive - rotate them periodically
# 5. .env.local is in .gitignore - check before committing
#
